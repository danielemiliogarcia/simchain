[package]
name = "simchainctl"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
minreq = "2"
serde = "1"
serde_json = "1"
simchain-common = { path = "../simchain-common" }

[dev-dependencies]
tempfile = "3"
