[package]
name = "simchain-network-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
simchain-common = { path = "../simchain-common" }
tiny_http = "0.12"
tracing = "0.1"
